import React, {useState} from 'react';
import './NewExpenseForm.css';

function BigAddExpenseButton(){
    return <div className="new-expense__actions">
        <button>Add Expense</button>
    </div>
}

export default BigAddExpenseButton;